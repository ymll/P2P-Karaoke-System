<Window x:Class="P2PKaraokeSystem.View.MainUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local_model="clr-namespace:P2PKaraokeSystem.Model"
        Title="P2P Karaoke System" Height="677" Width="1289" Icon="..\View\UIMaterial\Image\icon.ico">
    <Window.DataContext>
        <local_model:KaraokeSystemModel />
    </Window.DataContext>
    <Grid Background="black" RenderTransformOrigin="0.874,0.372">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Video -->
        <MediaElement x:Name="videoEle"
            HorizontalAlignment="Left" VerticalAlignment="Top"
            Height="505" Width="960"
            Margin="-2,0,0,0"/>

        <!-- Play Button -->
        <Image x:Name="playImg" Source="..\View\UIMaterial\Image\play.png"
               Grid.Row="2"
               HorizontalAlignment="Left" VerticalAlignment="Top" 
               Height="76" Width="80"
               Margin="435,0,0,0"
               MouseEnter="playBtn_MouseEnter" MouseLeave="playBtn_MouseLeave" MouseDown="playBtn_Click"/>

        <!-- Backward Button -->
        <Image x:Name="backwardImg" Source="..\View\UIMaterial\Image\backward.png"
               Grid.Row="2"
               HorizontalAlignment="Left" VerticalAlignment="Top"
               Height="31" Width="65"
               Margin="365,29,0,0"
               MouseEnter="backwardBtn_MouseEnter" MouseLeave="backwardBtn_MouseLeave" />

        <!-- Forward Button -->
        <Image x:Name="fastforwardImg" Source="UIMaterial/Image/fastforward.png"
               Grid.Row="2"
        	HorizontalAlignment="Left" VerticalAlignment="Top"
        	Height="31" Width="65"
        	Margin="535,29,0,0"
        	MouseEnter="fastforwardBtn_MouseEnter" MouseLeave="fastforwardBtn_MouseLeave" />


        <!-- Sound Button -->
        <Image x:Name="soundImg" Source="UIMaterial/Image/volumeup.png"
                Grid.Row="2"
        	    HorizontalAlignment="Left" VerticalAlignment="Top"
        	    Height="31" Width="40"
        	    Margin="30,29,0,0"
        	    MouseEnter="soundBtn_MouseEnter" MouseLeave="soundBtn_MouseLeave" MouseDown="soundBtn_Click"/>

        <!-- Lyric Box -->
        <Label x:Name="lyric"
               Grid.Row="1"
               Background="Black"
               Foreground="White" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center"
               HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center"
               Margin="0,5,328,0" Height="50" Width="960">
            <Label.Content>
                <AccessText x:Name="lyricText" TextWrapping="Wrap" Text="Here is the place for lyric"/>
            </Label.Content>
        </Label>

        <!-- Music List-->

        <ScrollViewer x:Name="musicList"
            Grid.RowSpan="3" Grid.ColumnSpan="2"
            HorizontalAlignment="Left" VerticalAlignment="Top"
            Margin="958,0,0,0"
            HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto"
            Background="Black">
            <Border BorderBrush="DeepPink" BorderThickness="2" Width="{DynamicResource {x:Static SystemParameters.VirtualScreenWidthKey}}" Height="{DynamicResource {x:Static SystemParameters.VirtualScreenHeightKey}}">
                <TextBlock Name="textBlock1"
                           Text="TextBlock TextBlockTextBlock"
                           Foreground="White"
                           TextWrapping="Wrap" />
            </Border>
        </ScrollViewer>



    </Grid>
</Window>
